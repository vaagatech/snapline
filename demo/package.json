{
  "name": "@vaagatech/demo",
  "private": true,
  "version": "0.1.0",
  "description": "Local demo for @vaagatech/reconcile framework",
  "type": "module",
  "scripts": {
    "build": "tsup && cp -R fixtures dist/",
    "start": "node dist/run-demo.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@vaagatech/api-adapters": "0.1.0",
    "@vaagatech/core": "0.1.0"
  }
}
